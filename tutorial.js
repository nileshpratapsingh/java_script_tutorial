const prompt = require("prompt-sync")(); // Importing prompt-sync

let friend = prompt("Enter your friend's name: "); // Prompting user to enter their friend's name
let friend2 = "rajesh"

console.log(`${friend} is the friend of ${friend2}`); // Printing a greeting message with the user's friend's 
